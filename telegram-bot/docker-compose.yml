services:
  bot:
    build:
      context: ..
      dockerfile: telegram-bot/Dockerfile
    env_file: .env
    network_mode: host
    restart: unless-stopped
    volumes:
      - bot-data:/app/telegram-bot/data

volumes:
  bot-data:
