{
  "name": "@agent-hq/review-agent",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc --noEmit",
    "test": "vitest run",
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "dependencies": {
    "@agent-hq/shared-types": "workspace:*",
    "@agent-hq/skills": "workspace:*",
    "@anthropic-ai/sdk": "^0.78.0",
    "@octokit/rest": "^22.0.1",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "vitest": "^4.0.18"
  }
}
