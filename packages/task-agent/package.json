{
  "name": "@agent-hq/task-agent",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc --noEmit",
    "test": "vitest run",
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "dependencies": {
    "@agent-hq/plane-client": "workspace:*",
    "@agent-hq/shared-types": "workspace:*",
    "@agent-hq/skills": "workspace:*",
    "@anthropic-ai/claude-agent-sdk": "^0.2.70",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "vitest": "^4.0.18"
  }
}
